apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: ptychography
spec:
  vertices:
    - name: in
      source:
        udsource:
          container:
            image: gemblerz/pvapy-udsource:0.0.1
            resources: {}
            imagePullPolicy: Always
            env:
            - name: PVA_INPUT_CHANNEL
              value: "pva:image"
            - name: PVA_QUEUE_SIZE
              value: "1000"
            - name: DEBUG
              value: "true"
      scale:
        min: 1
        max: 5
    - name: cat
      udf:
        container:
          image: gemblerz/ptychonn-udf:0.0.1
          resources: {}
          imagePullPolicy: Always
      scale:
        min: 1
        max: 15
      limits:
        readBatchSize: 1
      runtimeClassName: nvidia
    - name: out
      sink:
        log: {}
        retryStrategy:
          onFailure: retry
      scale: {}
      updateStrategy: {}
  edges:
    - from: in
      to: cat
    - from: cat
      to: out
